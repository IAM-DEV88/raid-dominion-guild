---
const { src = '/baul.png', alt = 'Logo', width="400", height="400" } = Astro.props;
---

<img class="mt-5 animate-slide-up-fade drop-shadow-4xl" src={src} alt={alt} width={width} height={height} />
